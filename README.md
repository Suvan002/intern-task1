# AI Startup Idea Validator

A simple Streamlit app that uses Hugging Face models (via LangChain) to analyze startup ideas and return a structured JSON report.

## Requirements

- Python 3.9+
- Hugging Face API token

## Installation

```bash
pip install streamlit langchain-huggingface huggingface_hub python-dotenv
